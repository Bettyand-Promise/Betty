# Backend API base URL (no trailing slash). The admin talks ONLY to the backend.
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
# Cloudinary cloud name (for signed direct uploads via the backend)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name
